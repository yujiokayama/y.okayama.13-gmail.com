{
  "name": "restapi_dev",
  "version": "1.0.0",
  "description": "REST APIのテスト環境",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1",
    "lowdb": "^1.0.0"
  }
}